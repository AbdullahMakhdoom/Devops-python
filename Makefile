install: 
	# install necessary pacakges 
	pip install --upgrade pip && pip install -r requirements.txt
format:
	# format python code
lint:
	# lint python code
tests:
	# run tests
build: 
	# build app
deploy:
	# deploy app
all: